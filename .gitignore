# Build artifacts
*.air
# NOTE: do NOT blanket-ignore *.swf — the game ships runtime .swf assets
# (e.g. textures_on_demand/.../maps/map_*.swf). Only ignore compiler output,
# which lives under bin-debug/ and bin-release/ (ignored below).

# IDE / OS
.idea/
.vscode/
*.iml
Thumbs.db
.DS_Store

# Flex compiler output
bin-debug/
bin-release/

# Cert / secrets (never commit)
cert/
*.p12
*.pfx
ant.properties
